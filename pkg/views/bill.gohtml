<!doctype html>
<html lang="en" class="h-screen">
<head>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@100..900&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="/static/css/styles.css">
    <script src="/static/js/bill.js" defer></script>
    <title>
        Bill
    </title>
</head>
<body class="bg-[--colour-900] h-screen font-noto">
<div class="flex flex-col justify-between items-center gap-[10vh] min-h-full overflow-y-auto text-[--colour-font]">
    <div class="flex flex-row bg-[--colour-800] w-full h-[10vh] items-center justify-between gap-16 px-32">
        You are Logged in as: User
        <div class="flex flex-row bg-[--colour-800] w-[50%] h-full text-[--colour-hyper] items-center justify-end gap-16">
            <a href="/items" class="text-center border border-[--colour-border] rounded-full w-[15%] h-[25%] bg-[--colour-900]">Items</a>
            <a href="/orders"
               class="text-center border border-[--colour-border] rounded-full w-[15%] h-[25%] bg-[--colour-900]">Orders</a>
            <a id="logout" href="/login"
               class="text-center border border-[--colour-border] rounded-full w-[15%] h-[25%] bg-[--colour-900]">Log Out</a>
        </div>
    </div>
    <div class="flex flex-col items-center justify-center border-gray-500 border border-[--colour-border] w-[50%] rounded-3xl">
        <div class="flex items-center justify-center rounded-t-3xl w-full h-[7vh] text-2xl font-bold">
            Order No: {{.Order.OrderId}}
        </div>
        <div class="flex flex-col items-center bg-[--colour-800] w-full">
            <div class="flex flex-row items-center justify-between w-full h-[7vh] border-gray-600 border border-[--colour-border] px-2">
                <div class="w-[20%]">
                    Item
                </div>
                <div class="w-[10%] text-right">
                    Price
                </div>
                <div class="w-[10%] text-right">
                    Count
                </div>
                <div class="w-[10%] text-right">
                    Cost
                </div>
            </div>
            {{range $i,$dish := .Order.Dishes}}
                <div class="flex flex-row items-center justify-between w-full h-[7vh] border-gray-600 border border-[--colour-border] px-2">
                    <div class="w-[20%]">
                        {{ $dish.ItemName }}
                    </div>
                    <div class="w-[10%] text-right">
                        {{ $dish.ItemPrice }}
                    </div>
                    <div class="w-[10%] text-right">
                        {{ $dish.DishCount }}
                    </div>
                    <div class="w-[10%] text-right">
                        ₹{{ $dish.Price }}
                    </div>
                </div>
            {{end}}
            <div class="flex flex-row items-center justify-between w-full h-[7vh] border-gray-600 border border-[--colour-border] px-2">
                <div class="w-[10%]">
                    Total
                </div>
                <div class="w-[20%]">
                </div>
                <div class="w-[10%] text-right">
                    ₹{{.Order.Price}}
                </div>
            </div>
        </div>
        <div class="flex flex-col h-[5vh] justify-center">
            {{if .Order.Paid}}
                Paid by {{.Username}}
            {{else}}
                Payment Pending by {{.Username}}
            {{end}}
        </div>
    </div>
    <div class="h-[10vh]"></div>
    <div class="align-end flex justify-end items-center bg-[--colour-800] w-full h-[10vh] px-8">
        <button id="exit"
                class="w-[10%] h-1/2 bg-[--colour-900] border-gray-500 border-4 rounded-full">
            Exit
        </button>
    </div>
</div>
</body>
</html>
