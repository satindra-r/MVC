<!doctype html>
<html lang="en" class="h-screen">
<head>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@100..900&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="/static/css/styles.css">
    <script src="/static/js/bill.js" defer></script>
    <title>
        Bill
    </title>
</head>
<body class="bg-slate-900 h-screen font-noto">
<div class="flex flex-col items-center gap-[10vh] overflow-y-auto">
    <div class="flex flex-row bg-slate-800 w-full h-[10vh] text-white items-center justify-between gap-16 px-32">
        You are Logged in as: User
        <div class="flex flex-row bg-slate-800 w-[50%] h-full text-teal-500 items-center justify-end gap-16">
            <a href="/items" class="text-center border rounded-full w-[15%] h-[25%]">Items</a>
            <a href="/orders" class="text-center border rounded-full w-[15%] h-[25%]">Orders</a>
            <a id="logout" href="/login" class="text-center border rounded-full w-[15%] h-[25%]">Log Out</a>
        </div>
    </div>
    <div class="flex flex-col items-center justify-center border-gray-500 border w-[50%] rounded-3xl text-white">
        <div class="flex items-center justify-center rounded-t-3xl w-full h-[7vh] text-2xl font-bold">
            Order No: {{.Order.OrderId}}
        </div>
        <div class="flex flex-col items-center bg-slate-800 w-full">
            <div class="flex flex-row items-center justify-between w-full h-[7vh] border-gray-600 border px-2">
                <div class="w-[20%]">
                    Item
                </div>
                <div class="w-[10%] text-right">
                    Price
                </div>
                <div class="w-[10%] text-right">
                    Count
                </div>
                <div class="w-[10%] text-right">
                    Cost
                </div>
            </div>
            {{range $i,$dish := .Order.Dishes}}
                <div class="flex flex-row items-center justify-between w-full h-[7vh] border-gray-600 border px-2">
                    <div class="w-[20%]">
                        {{ $dish.ItemName }}
                    </div>
                    <div class="w-[10%] text-right">
                        {{ $dish.ItemPrice }}
                    </div>
                    <div class="w-[10%] text-right">
                        {{ $dish.DishCount }}
                    </div>
                    <div class="w-[10%] text-right">
                        ₹{{ $dish.Price }}
                    </div>
                </div>
            {{end}}
            <div class="flex flex-row items-center justify-between w-full h-[7vh] border-gray-600 border px-2">
                <div class="w-[10%]">
                    Total
                </div>
                <div class="w-[20%]">
                </div>
                <div class="w-[10%] text-right">
                    ₹{{.Order.Price}}
                </div>
            </div>
        </div>
        <div class="flex flex-col h-[5vh] justify-center">
            {{if .Order.Paid}}
                Paid by {{.Username}}
            {{else}}
                Payment Pending by {{.Username}}
            {{end}}
        </div>
    </div>
    <div class="h-[10vh]"></div>

</div>
</body>
</html>
