# MVC

run `docker compose up` to start the server

run `./test.sh` to run testbench

## Credentials
```
Username:user1
Password:password

Username:user2
Password:password

Username:chef
Password:password

Username:admin
Password:password
```